# ── Flask Core ───────────────────────────────────────────────


# ── Email (SMTP or Dev Suppress) ─────────────────────────────

# ── CORS / Origins ───────────────────────────────────────────

# ── Database ─────────────────────────────────────────────────

# ── CSRF & Forms ─────────────────────────────────────────────

# ── Stripe (Test Mode Keys — replace for live) ────────────────

# ── PayPal Sandbox (optional) ────────────────────────────────

# ── API Auth ─────────────────────────────────────────────────

# ── Redis / Socket.IO (Realtime) ─────────────────────────────

# ── Rate limiting & Logs ─────────────────────────────────────

# ── Feature Flags ────────────────────────────────────────────

# ── Sentry / Slack / OpenAI (Optional) ───────────────────────

# --- FundChamps payments/metrics ---




FLASK_ENV=production
FLASK_CONFIG=app.config.DevelopmentConfig
FLASK_DEBUG=1
SECRET_KEY=dev_only_CHANGE_ME
DATABASE_URL=sqlite:///app/data/app.db
STRIPE_SECRET_KEY=sk_test_************************
STRIPE_API_KEY=sk_test_xxx       # your test key
STRIPE_WEBHOOK_SECRET=whsec_xxx  # leave blank while offline
