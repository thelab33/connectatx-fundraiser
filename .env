# ── Flask Core ───────────────────────────────────────────────
FLASK_ENV=development
FLASK_DEBUG=1
FLASK_CONFIG=app.config.DevelopmentConfig
SECRET_KEY=CHANGE_ME_dev_only_please

# Optional: host/port for fundchamps_run.py
HOST=0.0.0.0
PORT=5000

MAIL_SERVER=localhost
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=
MAIL_PASSWORD=

# ── CORS / Origins ───────────────────────────────────────────
# allow everything in dev; override per-need
CORS_ORIGINS=*
PRIMARY_ORIGIN=http://127.0.0.1:5000

# ── Database (SQLite dev) ────────────────────────────────────
# .env
DATABASE_URL=sqlite:////home/cyberboyz/connectatx-fundraiser/app/data/app.db
SQLALCHEMY_ECHO=0

# ── CSRF (forms ON; API exempted in code) ────────────────────
WTF_CSRF_ENABLED=1
WTF_CSRF_TIME_LIMIT=43200

# ── Stripe (dev: keep empty until you have keys) ─────────────
STRIPE_SECRET_KEY=
STRIPE_PUBLIC_KEY=
STRIPE_WEBHOOK_SECRET=
DOMAIN=http://127.0.0.1:5000

# ── PayPal Sandbox ───────────────────────────────────────────
PAYPAL_ENV=sandbox
PAYPAL_CLIENT_ID=
PAYPAL_SECRET=
PAYPAL_TIMEOUT=15

# ── Auth for /api (either static tokens or JWT) ──────────────
API_TOKENS=devtoken123
JWT_ALG=HS256
JWT_SECRET=change_me_dev
JWT_PUBLIC_KEY=
API_AUDIENCE=https://fundchamps.api
API_ISSUER=https://auth.local/

# ── Email (dev: suppress send) ───────────────────────────────
MAIL_SUPPRESS_SEND=1
MAIL_DEFAULT_SENDER="Connect ATX Elite <noreply@local.test>"
MAIL_SERVER=
MAIL_PORT=
MAIL_USE_TLS=
MAIL_USERNAME=
MAIL_PASSWORD=

# ── Redis / Socket.IO (optional in dev) ──────────────────────
REDIS_URL=redis://localhost:6379/0
CACHE_TYPE=simple
CACHE_REDIS_URL=
SOCKETIO_ASYNC_MODE=threading
SOCKETIO_MESSAGE_QUEUE=

# ── Rate limiting / misc ─────────────────────────────────────
LIMITER_REDIS_URL=memory://
LOG_LEVEL=DEBUG
WERKZEUG_LOG_LEVEL=WARNING
FEATURE_CONFETTI=true
FEATURE_DARK_MODE=true
FEATURE_AI_THANK_YOU=false
PRINT_CONFIG=false

# ── Sentry / Slack (off in dev) ──────────────────────────────
SENTRY_DSN=
SENTRY_TRACES=0.0
SLACK_WEBHOOK_URL=
OPENAI_API_KEY=

